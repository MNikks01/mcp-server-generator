{
  "name": "@mnikks01/mcpforge",
  "version": "0.1.2",
  "type": "module",
  "description": "MCPForge generation engine + CLI (Phase A) — OpenAPI spec -> production-ready MCP server. Runs on Node 24 native TypeScript; zero runtime dependencies.",
  "bin": {
    "mcpforge": "dist/cli.js"
  },
  "scripts": {
    "demo": "node scripts/demo.ts",
    "test": "node --test test/*.test.ts",
    "cli": "node src/cli.ts",
    "sync-engine": "node scripts/sync-engine.mjs",
    "sync-engine:check": "node scripts/sync-engine.mjs --check",
    "build": "npx --yes esbuild@0.24.2 src/cli.ts --bundle --platform=node --format=esm --target=node18 --packages=external --outfile=dist/cli.js",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MNikks01/mcp-server-generator.git",
    "directory": "mcpforge"
  },
  "homepage": "https://github.com/MNikks01/mcp-server-generator#readme",
  "bugs": "https://github.com/MNikks01/mcp-server-generator/issues",
  "publishConfig": {
    "access": "public"
  }
}
