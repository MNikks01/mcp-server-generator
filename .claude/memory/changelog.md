# Changelog (engineering notes) — MCP Server Generator

User-facing releases live in the root `CHANGELOG.md`. This file is for engineering notes between releases.

- 0.1.0 — initial engine + mcpforge/ (engine), mcpforge/src/cli.ts (CLI), web/ (Next.js app); tests + CI + security + .claude workspace.
